# Task definition

Create a Python HTTP API, that allows users to register with their information and interests. Then, everybody should be able to see the list of registered users. There should also be a search functionality to find users based on their interests. The API should be built using FastAPI and use an in-memory database (like a list or dictionary) to store user information.
Use uv for package management and running the server.